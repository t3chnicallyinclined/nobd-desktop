[workspace]
members = ["app", "shared", "vigem-sync"]
resolver = "2"

# Universal sync replaced the per-game DINPUT8.dll, so the cdylib package is gone.
[profile.release]
opt-level = 2
lto = true
strip = true
